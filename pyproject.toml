[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "lather"
version = "0.1.0"
description = "SDK de processamento de dados para IA"
requires-python = ">=3.14"